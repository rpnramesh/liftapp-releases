// ── STORAGE KEYS ─────────────────────────────────────────
// All AsyncStorage keys used across the app.
// Using constants avoids typos in key names.

const STORAGE_KEYS = {
  weightLog:      'lift_weight_log',
  measureData:    'lift_measure_data',
  workoutRecords: 'lift_workout_records',
  restDays:       'lift_rest_days',
};

export default STORAGE_KEYS;